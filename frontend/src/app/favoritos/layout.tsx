import { ProtectedContent } from "@/shared/components/ProtectedContent";
export default function FavoritesLayout({ children }: { children: React.ReactNode }) { return <ProtectedContent>{children}</ProtectedContent>; }
